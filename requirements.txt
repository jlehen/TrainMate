# Core
Flask>=3.0
PyYAML>=6.0
requests>=2.31

# Google Calendar sync (service account)
google-api-python-client>=2.0
google-auth>=2.0

# Garmin Connect (direct daily-metrics & activity sync)
garminconnect>=0.2.0

# Telegram chat front-end (trainmate_bot.py); optional — only needed to run the bot
python-telegram-bot>=21.0
